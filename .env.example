# ===========================================
# 汗蒸会员管理系统 - 环境变量配置
# ===========================================

# -----------------
# 数据库配置
# -----------------
# 直接运行时使用 127.0.0.1
# Docker 部署时需要改为:
#   - Linux: 172.17.0.1
#   - Mac/Windows: host.docker.internal
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=sauna_membership
DB_CONNECTION_LIMIT=10
# DB_CONNECT_TIMEOUT=5000
# DB_HOST_FALLBACKS=host.docker.internal,172.17.0.1

# -----------------
# 服务配置
# -----------------
PORT=4000
CORS_ORIGIN=http://localhost:3000,http://127.0.0.1:3000

# -----------------
# 安全配置
# -----------------
JWT_SECRET=change_me_to_random_secret_key_at_least_32_characters
BCRYPT_SALT_ROUNDS=10

# -----------------
# 前端配置（仅开发环境需要）
# -----------------
# 生产环境会自动使用相对路径 /api，无需设置
# 开发环境如果需要连接不同的后端，可以设置：
# VITE_API_URL=http://localhost:4000/api
